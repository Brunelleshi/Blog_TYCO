<h1><%= t('app.edit.title', model: Article.model_name.human) %></h1>
<!--caixas para edição do artigo-->
<%= render 'form', article: @article %>
<!--cancela e retorna a postagem-->
<%= link_to 'Cancel', article_path(@article), class: 'btn btn-secondary' %>